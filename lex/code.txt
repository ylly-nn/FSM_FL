
{   a:=1;
    a, a, c, b : %;
    x : !;
    flag : $;
    i: %;

    a:=(2);
    flag:=3 NE true;
    readln a, b;

    c := b plus x mult b plus 10;
    writeln c;

    c:=x;

    a:=1.1;
    x:=flag;

    

    flag := true;

    if (a plus 1) begin
        writeln c;
        x := ~~a
    end else begin
        writeln 0;
        flag := false
    end;
    for i:=1.1 to 1.1 step 1.1 a:=21 next;

    while (a min 1) a:=1;

    x:=1.1E+2;
    readln a, b, c;
 
    
}

