
{   a, c, b : %;
    x : !;
    flag : $;
    i: %;

    a:=(2);
    flag:=3 NE true;
    readln a, b;

    c := b plus x mult b plus 10;
    writeln c;

    c:=x;

    a:=1.1;
    x:=flag;

    

    flag := true;

    if (a EQ 1) begin
        writeln c;
        x := ~~a
    end else begin
        writeln 0;
        flag := false
    end;
    for i:=1 to 2 step 1 a:=2 next;

    while (a EQ 1) a:=1;

    x:=1.1E+2;
    readln a, b, c;
 
    
}

